export type User = {
  name: string;
  email: string;
  phoneNumber: number;
};

export type Plan = {
  name: string;
  monthPrice: number;
};

export type AddOn = {
  name: string;
  description: string;
  monthPrice: number;
};
